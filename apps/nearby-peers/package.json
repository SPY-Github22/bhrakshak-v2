{
  "name": "bhrakshak-nearby-peers",
  "version": "1.0.0",
  "description": "Portable rescuer⇄citizen proximity discovery: consent-gated GPS announce/query over any IP link + offline BLE beacon scanning. No runtime dependencies; React is a peer dependency for the UI panels.",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "selfcheck": "node --experimental-strip-types tests/selfcheck.ts"
  },
  "peerDependencies": {
    "react": ">=17"
  },
  "devDependencies": {
    "@types/react": "^18.3.3",
    "typescript": "^5.5.3"
  }
}
